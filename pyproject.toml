[project]
name = "design"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "crawl4ai>=0.6.3",
    "fastapi>=0.115.12",
    "uvicorn>=0.34.2",
    "colorgram.py>=1.2.0",  # added for color extraction
]
